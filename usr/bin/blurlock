#!/bin/bash
# /usr/bin/blurlock
logger -s "Blurlock is deprecated, use 'xautolock -locknow'"
# lock the screen
i3lock 
# sleep 1 adds a small delay to prevent possible race conditions with suspend
sleep 1

exit 0
